<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CollaredDoves" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/secondary_dark</item>
        <item name="colorPrimaryVariant">@color/secondary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="appToolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="materialThemeOverlay">@style/appToolbarThemeOverlay</item>
        <item name="titleTextAppearance">@style/appToolbarTitle</item>
        <item name="subtitleTextAppearance">@style/appToolbarSubtitle</item>
    </style>

    <style name="appToolbarTitle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="fontFamily">@font/yanone_kaffeesatz</item>
        <item name="android:fontFamily">@font/yanone_kaffeesatz</item>
    </style>

    <style name="appToolbarSubtitle" parent="TextAppearance.MaterialComponents.Subtitle1">
        <item name="fontFamily">@font/yanone_kaffeesatz</item>
        <item name="android:fontFamily">@font/yanone_kaffeesatz</item>
    </style>

    <style name="appToolbarThemeOverlay" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <item name="colorPrimary">@color/secondary</item>
        <item name="colorPrimaryVariant">@color/secondary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
</resources>